#!/usr/bin/env python
import sys

print(sys.version_info)
assert sys.version_info[:2] == (3, 5)

import platform
assert platform.python_implementation() == 'PyPy'

import tornado

# Verify we are primary version!
import jupyter
