#!/usr/bin/env python
import sys

print(sys.version_info)
assert sys.version_info[:2] == (3, 4)

import numpy

# Validate we are *not* in the primary environment
try:
    import notebook
    assert False
except ImportError:
    pass
